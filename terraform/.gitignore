# Local Terraform state files
*.tfstate
*.tfstate.*

# Local Terraform configuration folder
.terraform/

# .tfvars files
# These often contain sensitive variables or environment-specific values.
# It's generally recommended to keep them out of version control,
# although *.auto.tfvars are usually an exception.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used for local testing or
# sensitive data and are environment-specific.
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include override files you do wish to source control.
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Terraform crash log
crash.log
crash.*.log
